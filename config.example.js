/* Copy to config.js and set your own API address.
 *
 * config.js is deliberately not committed: it is per-deployment, not per-repo.
 * Without it the page still works — the sign-in form asks for the address and
 * remembers it — so this only saves whoever uses the page from typing it.
 */
window.TOEM_CONFIG = {
  apiUrl: "https://toemapi.example.com",
};
